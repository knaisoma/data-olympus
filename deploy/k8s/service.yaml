apiVersion: v1
kind: Service
metadata:
  name: data-olympus-mcp
  namespace: data-olympus
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: data-olympus-mcp
